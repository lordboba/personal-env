#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
DOWNLOAD_DMG="$ROOT_DIR/download-site/public/downloads/Personal-Env-macOS.dmg"

bash "$ROOT_DIR/scripts/package-macos.sh" >/tmp/personal-env-post-commit.log 2>&1
git -C "$ROOT_DIR" add "$DOWNLOAD_DMG"

echo "post-commit: staged $DOWNLOAD_DMG"
